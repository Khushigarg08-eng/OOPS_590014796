public class Code3 {
    public static void main(String[] args) {

        // Storing command line arguments into a String array
        String[] arr = args;

        // Printing the array elements
        System.out.println("Array elements are:");
        for (int i = 0; i < arr.length; i++) {
            System.out.println(arr[i]);
        }
    }
}

